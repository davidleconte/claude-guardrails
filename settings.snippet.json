{
  "//": "Merge these entries into ~/.claude/settings.json (adjust $GUARDRAILS_HOME). The generic precheck-gate has NO `if` filter — the engine matches each profile's trigger against the actual command — so it coexists with rtk hook claude and any tier1-gate in the same PreToolUse/Bash array. Set GUARDRAILS_HOME to the install dir of this prototype.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/guardrails/hooks/precheck-gate.sh\"",
            "timeout": 60
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/guardrails/hooks/session-review.sh\"",
            "timeout": 30
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/guardrails/hooks/policy-drift.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
