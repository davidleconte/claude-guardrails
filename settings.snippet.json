{
  "//": "Merge these entries into ~/.claude/settings.json. Paths assume the install location ~/Projects/claude-guardrails. The generic precheck-gate has NO `if` filter — the engine matches each profile's trigger against the actual command — so it coexists with `rtk hook claude` and any `tier1-gate` already in the same PreToolUse/Bash array (add, don't replace).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/Projects/claude-guardrails/hooks/precheck-gate.sh\"",
            "timeout": 60
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/Projects/claude-guardrails/hooks/session-review.sh\"",
            "timeout": 30
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/Projects/claude-guardrails/hooks/policy-drift.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
